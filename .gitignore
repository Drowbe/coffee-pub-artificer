# FoundryVTT Module .gitignore

# OS Files
.DS_Store
Thumbs.db
desktop.ini

# Editor Files
.vscode/
.idea/
*.swp
*.swo
*~
*.code-workspace

# Node (if you add build tools later)
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
build/

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Foundry compendium pack: exclude only runtime lock file (LOCK).
# Do NOT ignore packs/*/CURRENT — LevelDB needs it to open the pack; without it compendiums appear empty when the module is installed from the release zip.
packs/*/LOCK

# Lock files (uncomment if you don't want to track these)
# *.lock

